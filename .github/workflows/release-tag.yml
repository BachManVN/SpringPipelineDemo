name: '[Release] Create Semantic Version Tag'
run-name: |
  ${{ format('[Release] Create Semantic Version Tag - Main branch push by {0}', github.actor) }}

# CRITICAL: This is the ONLY authoritative tag creation path for normal releases
# WHY: Tags decide WHAT is deployed - only CI should create deployable tags
# This creates release tags automatically when code is merged to main branch

on:
  push:
    branches:
      - main

env:
  JAVA_VERSION: '17'

jobs:
  create-release-tag:
    name: Create Release Tag
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required to create tags
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history and tags
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify main branch
        run: |
          if [ "${{ github.ref_name }}" != "main" ]; then
            echo "❌ ERROR: This workflow should only run on 'main' branch"
            exit 1
          fi
          echo "✅ Workflow triggered by main branch push"

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Determine next release version
        id: version
        run: |
          echo "🔍 Determining next release version..."
          git fetch --tags --force
          
          LATEST_TAG=$(git tag -l "v[0-9]*.[0-9]*.[0-9]*" | sort -V | tail -1 || echo "v0.0.0")
          echo "📌 Latest version: $LATEST_TAG"
          
          if [[ "$LATEST_TAG" =~ ^v([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            MAJOR="${BASH_REMATCH[1]}"
            MINOR="${BASH_REMATCH[2]}"
            PATCH="${BASH_REMATCH[3]}"
          else
            MAJOR=0
            MINOR=1
            PATCH=0
          fi
          
          # Default to minor version bump
          NEW_MINOR=$((MINOR + 1))
          NEW_VERSION="v${MAJOR}.${NEW_MINOR}.0"
          
          echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "✅ Next version: $NEW_VERSION"

      - name: Create and push tag
        run: |
          NEW_VERSION="${{ steps.version.outputs.NEW_VERSION }}"
          
          # Check if tag already exists
          if git rev-parse "$NEW_VERSION" >/dev/null 2>&1; then
            EXISTING_COMMIT=$(git rev-parse "$NEW_VERSION")
            CURRENT_COMMIT=$(git rev-parse HEAD)
            if [ "$EXISTING_COMMIT" != "$CURRENT_COMMIT" ]; then
              echo "❌ ERROR: Tag $NEW_VERSION already exists and points to different commit"
              exit 1
            fi
            echo "✅ Tag $NEW_VERSION already exists at current commit"
            exit 0
          fi
          
          echo "📦 Creating annotated tag: $NEW_VERSION"
          git tag -a "$NEW_VERSION" -m "Release $NEW_VERSION"
          git push origin "$NEW_VERSION"
          echo "✅ Tag $NEW_VERSION created and pushed"
